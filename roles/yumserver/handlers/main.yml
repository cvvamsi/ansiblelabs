---
# handlers file for yumserver
- name: Restarting vsftpd service
  service:
    name: vsftpd
    enabled: yes
    state: restarted

- name: Restart firewalld
  systemd:
    name: firewalld
    daemon_reload: yes
    name: firewalld

